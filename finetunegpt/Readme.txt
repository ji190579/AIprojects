In this example, we are fine-tuning ChatGPT to provide entities limited to our list of values.
We use Pydantic’s JSON schema, and in the example, we have a cell for the trial without fine-tuning and a new one with fine-tuning.
Additionally, we include code to calculate the input and output tokens and the associated cost.
